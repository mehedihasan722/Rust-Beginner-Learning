/// A favorite color enum.
enum Color {
    Red,
    Green,
    Blue,
}

// A Piece of mail.

struct Mail {
    /// The Destination address of the mail.
    address: String,
}

/// Add two integers and return the result.
fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn main() {}
